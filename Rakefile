# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'ffi-chm' do
  developer('nanki', 'nanki@dotswitch.net')
  extra_deps << ['ffi']
  extra_deps << ['bindata']
  extra_deps << ['nokogiri', '>=1.5.0']
end

# vim: syntax=ruby
